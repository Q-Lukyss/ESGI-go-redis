# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Binaire WASM généré par `GOOS=js GOARCH=wasm go build -o web/public/goredis.wasm ./cmd/wasm`
# — build artifact, pas du source. wasm_exec.js reste committé (glue runtime
# Go, quasi jamais modifiée) pour simplifier le clone-and-run.
public/goredis.wasm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
